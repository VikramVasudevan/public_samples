function helloWorld(){ return JSON.stringify(['Hello World','How are you?']); } 

function generateNumbers(){ return JSON.stringify([ { 'random_number' : Math.random(), 'some_message':'How are you?'} ]); }
